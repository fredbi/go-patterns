package pipelines

import (
	// "github.com/stretchr/testify/require"
	"testing"
)

func TestPipeline(t *testing.T) {
	/*
		p := New[int, int, struct{}]("my pipe")

		_, ok := p.(interface{ Consumer[int] })
		require.True(t, ok)
	*/
}
